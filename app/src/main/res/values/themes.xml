<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NewsApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_color</item>
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/App.Custom.Text</item>
    </style>

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/accent_color</item>
    </style>

    <style name="App.Custom.Text" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/on_background_color</item>
    </style>

    <style name="App.Custom.Text.Secondary">
        <item name="android:textColor">@color/on_background_secondary_color</item>
    </style>

</resources>